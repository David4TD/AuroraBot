# AuroraBot — runtime dependencies
# 2.5.0+ is required for application emojis (team logo icons).
discord.py==2.7.1
aiohttp==3.9.5
aiosqlite==0.20.0
python-dotenv==1.0.1
# Normalises team logos to small PNGs before upload. Discord's emoji endpoint
# rejects WebP and oversized assets with "Invalid Asset" (50046), and PandaScore
# serves a mix of formats and dimensions.
Pillow==12.3.0
# zoneinfo needs a tz database; python:*-slim does not reliably ship one,
# and DIGEST_TZ resolution must not depend on the base image.
tzdata==2025.2
